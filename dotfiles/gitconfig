[user]
    name = MacRae Linton
    email = macrae.linton@gmail.com

[alias]
    pushcur = ! git branch | grep *
    branchprune = "! git branch --merged | egrep -v \"(^\\*|master)\""
    pushcurs = ! git branch | grep *
